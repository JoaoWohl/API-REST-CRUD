spring.application.name=api

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.url=${SPRING_DATASOURCE_URL:${DB_URL}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${DB_USER}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD}}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.web.resources.add-mappings=false

server.servelet.session.cookie.http-only: true

JWT_SECRET=${JWT_SECRET}

# Configuracoes para o Email Sender

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=joaovitorsantoswohl9@gmail.com
spring.mail.password=fnxymyiyvvhcsajm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true